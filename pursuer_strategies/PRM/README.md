# PRM算法测试指南
- 直接在根目录运行 `generator.py` 文件，在文件中可以通过参数调整上算法和地图生成，可以测试单一算法和地图，并直接生成可视化结果
- 直接在根目录运行 `evalutor.py` 文件，可以运行自动化测评结果，在当前路径下生成 results 文件夹保存不同地图的路图文件以及记录各项指标的 csv 文件
- 直接在根目录运行 `analysis.py` 文件，可以运行数据处理程序，依然在 results 文件夹下的 chart 文件夹生成各项指标的柱状图